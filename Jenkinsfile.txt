pipeline {
    agent any
    stages {
    stage('PMD') {
            steps {
                sh 'mvn site'
            }
        }
      stage('Clean') {
          steps {
                sh 'mvn clean'
          }
      }
		//Code ends for stage Clean
    }
}
